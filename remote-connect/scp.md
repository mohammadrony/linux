# SCP

Send Files

- `scp ~/my-files.txt user@host:~/`
- `scp -i secret.pem ~/my-files.txt user@host:~/`

Download Files

- `scp user@host:~/my-files.txt ~/`
- `scp -i secret.pem user@host:~/my-files.txt ~/`
