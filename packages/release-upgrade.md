# Release Upgrade

Update package

```bash
sudo apt update
sudo apt upgrade -y
```

Upgrade operating system

```bash
sudo do-release-upgrade
```

Update all held back package

```bash
sudo apt update
sudo apt full-upgrade -y
```

Restart

```bash
sudo reboot now
```
